-- Script that lists all records of a table
-- Query to list all reords of the table second_table which have the name value
SELECT score, name FROM second_table WHERE name IS NOT NULL ORDER BY score DESC;
